```mdx
---
title: 'Sign In'
api: 'POST /signin'
description: 'Authenticate a user with email and password'
---

Endpoint

```
POST /make-server-4010be06/signin
```

Sign in an existing user with email and password credentials.

---

Request Body

<ParamField body="email" type="string" required>
  User's email address
</ParamField>

<ParamField body="password" type="string" required>
  User's password
</ParamField>

---

Response

<ResponseField name="session" type="object">
  Supabase auth session object
  
  <Expandable title="session properties">
    <ResponseField name="access_token" type="string">
      JWT access token for authenticated requests
    </ResponseField>
    
    <ResponseField name="refresh_token" type="string">
      Token to refresh the access token
    </ResponseField>
    
    <ResponseField name="expires_in" type="number">
      Session expiration time in seconds
    </ResponseField>
    
    <ResponseField name="user" type="object">
      User authentication data
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="user" type="object">
  Authenticated user object from Supabase Auth
</ResponseField>

---

Example Request

```typescript
const response = await fetch('https://YOUR_PROJECT.supabase.co/functions/v1/make-server-4010be06/signin', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    email: 'john@example.com',
    password: 'securepassword123'
  })
})

const data = await response.json()
console.log(data.session.access_token)
```

---

Example Response

```json
{
  "session": {
    "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "refresh_token": "v1.MThiOGI0NDYtOD...",
    "expires_in": 3600,
    "token_type": "bearer",
    "user": {
      "id": "123e4567-e89b-12d3-a456-426614174000",
      "email": "john@example.com",
      "created_at": "2024-01-15T10:30:00Z"
    }
  },
  "user": {
    "id": "123e4567-e89b-12d3-a456-426614174000",
    "email": "john@example.com",
    "created_at": "2024-01-15T10:30:00Z"
  }
}
```

---

Error Responses

<ResponseExample>
```json 400 - Invalid Credentials
{
  "error": "Invalid login credentials"
}
```

```json 500 - Server Error
{
  "error": "Failed to sign in"
}
```
</ResponseExample>

---

Notes

<Note>
After successful sign in, store the `access_token` securely and include it in the Authorization header for subsequent API requests:

```
Authorization: Bearer YOUR_ACCESS_TOKEN
```
</Note>

<Warning>
Never expose access tokens in client-side code or logs. Store them securely in memory or secure storage.
</Warning>

---

Next Steps

<CardGroup cols={2}>
  <Card title="Get Profile" icon="user" href="/api-reference/profile/get-profile">
    Fetch user profile data
  </Card>
  
  <Card title="Create Task" icon="plus" href="/api-reference/tasks/create-task">
    Create your first task
  </Card>
  
  <Card title="Sign Out" icon="arrow-right-from-bracket" href="/api-reference/auth/signout">
    End user session
  </Card>
  
  <Card title="Check Session" icon="shield-check" href="/api-reference/auth/session">
    Verify active session
  </Card>
</CardGroup>
```
